package recycling;

import java.util.Comparator;

/**
 * The CompareByWeight class implements the Comparator Interface which defines a method to compare
 * two items by their weight.
 */
public class CompareByWeight implements Comparator<Item> {

  @Override public int compare(Item o1, Item o2) {
    // TODO: Implement this method
    return 0;
  }
}
